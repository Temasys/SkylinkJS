<div id="method_{{name}}" class="code-item method item {{#if access}}{{access}}-{{/if}}method-item {{#if deprecated}} deprecated{{/if}}{{#if extended_from}} inherited{{/if}}">
  <h3 class="code-item-name"><code>{{name}}</code></h3>
  {{#if params}}
    <div class="args">
      <span class="paren">(</span>
      {{#params}}
        {{#if optional}}
          <code class="optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
        {{else}}
          <code>{{name}}</code>
        {{/if}}
        <i class="seperator">,&nbsp;&nbsp;</i>
      {{/params}}
      <span class="paren">)</span>
    </div>
  {{else}}
    <span class="paren">()</span>
  {{/if}}

  <div class="code-item-properties">
    <div class="properties-wrapper">
      {{#if deprecated}}
        <span class="label label-warning" title="This property is deprecated. Might be removed in future releases.">deprecated</span>
      {{/if}}

      {{#if multiple}}
        <span class="label label-warning" title="This argument may occur one or more times.">multiple</span>
      {{/if}}

      {{#if unsupported}}
        <span class="label label-warning" title="This feature may not be supported in the specs. Kept for future releases.">unsupported</span>
      {{/if}}

      {{#if access}}
        <span class="label label-default" title="This method is {{access}}.">{{access}}</span>
      {{/if}}

      {{#if final}}
        <span class="label label-danger" title="This method is final and cannot be changed.">final</span>
      {{/if}}

      {{#if overwritten_from}}
        <span class="label label-danger" title="This method is overwritten.">overwritten</span>
      {{/if}}

      {{#if development}}
        <span class="label label-danger label-development" title="This feature is still in development. In may not be available in the current release.">in-development</span>
      {{/if}}

      {{#if static}}
        <span class="label label-default" title="This method is static.">static</span>
      {{/if}}

      {{#if chainable}}
        <span class="label label-info" title="This method is chainable.">chainable</span>
      {{/if}}

      {{#if async}}
        <span class="label label-info" title="This method is asynchronous.">async</span>
      {{/if}}

      {{#if global}}
        <span class="label label-info label-global" title="This method is placed in the global class namespace.">global</span>
      {{/if}}
    </div>

    <div class="code-item-information">
      {{#if overwritten_from}}
        <span>
          Inherited from
          <a href="{{crossLinkRaw overwritten_from/class}}#method_{{overwritten_from/name}}">{{overwritten_from/class}}</a>
          {{#if foundAt}}
            but overwritten in
          {{/if}}
        </span>
      {{else}}
        {{#if extended_from}}
          <span>
            Inherited from:
            <a href="{{crossLinkRaw extended_from}}#method_{{name}}">{{extended_from}}</a>
          </span>
        {{else}}
          {{#providedBy}}
            <span>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</span>
          {{/providedBy}}
        {{/if}}
      {{/if}}
      {{#if deprecationMessage}}
        <span>Deprecated: {{deprecationMessage}}</span>
      {{/if}}
      {{#if providedBy}}
        {{#providedBy}}
          <span>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</span>
        {{/providedBy}}
      {{/if}}
    </div>

    {{#if foundAt}}
      <small>
        <i class="fa fa-chevron-right"></i>&nbsp;&nbsp;Defined in: <a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>
      </small>
    {{/if}}

    {{#if since}}
      <small><i class="fa">&nbsp;&nbsp;&nbsp;</i>&nbsp;&nbsp;Available since <span class="badge">{{since}}</span></small>
    {{/if}}
  </div>

  <div class="code-item-description">
    {{{methodDescription}}}
  </div>

  {{#if trigger}}
  <div class="code-item-section">
    <h4>Triggers:</h4>
    <span class="code-item-trigger">{{trigger}}</span>
  </div>
  {{/if}}

  {{#if params}}
    <div class="code-item-section">
      <h4>Parameters:</h4>

      <ul class="params-list">
      {{#params}}
          <li class="param">
            <code class="param-name">{{name}}</code>
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

            {{#if optional}}<span class="label">Optional</span>{{/if}}
            {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

            <div class="param-description">
              {{#if optdefault}}<small class="default"><span>DEFAULT: </span>{{optdefault}}</small>{{/if}}
              {{{description}}}
            </div>

            {{#if props}}
              <ul class="params-list">
                {{#props}}
                  <li class="param">
                    <code class="param-name">{{name}}</code>
                    <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

                    {{#if optional}}<span class="label">Optional</span>{{/if}}
                    {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

                    <div class="param-description">
                      {{#if optdefault}}<small class="default"><span>DEFAULT: </span>{{optdefault}}</small>{{/if}}
                      {{{description}}}
                    </div>

                    {{#if props}}
                      <ul class="params-list">
                        {{#props}}
                          <li class="param">
                            <code class="param-name">{{name}}</code>
                            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

                            {{#if optional}}<span class="label">Optional</span>{{/if}}
                            {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

                            <div class="param-description">
                              {{#if optdefault}}<small class="default"><span>DEFAULT: </span>{{optdefault}}</small>{{/if}}
                              {{{description}}}
                            </div>
                          </li>
                        {{/props}}
                      </ul>
                    {{/if}}
                  </li>
                {{/props}}
              </ul>
            {{/if}}
          </li>
      {{/params}}
      </ul>
    </div>
  {{/if}}

  {{#return}}
    <div class="code-item-section">
      <h4>Returns:</h4>

      <div class="returns-description">
        {{#if description}}
          {{#if type}}
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>:
          {{/if}}
          {{{description}}}
        {{else}}
          {{#if type}}
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>:
          {{/if}}
        {{/if}}
      </div>
    </div>
  {{/return}}

  {{#example}}
    <div class="code-item-section">
      <h4>Example:</h4>
      <pre class="example-content prettyprint">
        {{{.}}}
      </pre>
    </div>
  {{/example}}
  <hr>
</div>