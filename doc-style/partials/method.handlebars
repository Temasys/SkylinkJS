<div id="method_{{name}}" class="code-item method item {{#if access}}{{access}}-{{/if}}method-item {{#if deprecated}} deprecated{{/if}}{{#if extended_from}} inherited{{/if}}">
  <h3 class="code-item-name"><code>{{name}}</code></h3>

  {{#if params}}
    <div class="args">
      <span class="paren">(</span>
      {{#params}}
        {{#if optional}}
          <code class="optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
        {{else}}
          <code>{{name}}</code>
        {{/if}}
        <i class="seperator">,&nbsp;&nbsp;</i>
      {{/params}}
      <span class="paren">)</span>
    </div>
  {{else}}
    <span class="paren">()</span>
  {{/if}}

  <div class="code-item-properties">
    {{#if deprecated}}
      <span class="label label-warning">deprecated</span>
    {{/if}}

    {{#if multiple}}
      <span class="label label-warning" title="This argument may occur one or more times.">multiple</span>
    {{/if}}

    {{#if access}}
      <span class="label label-default">{{access}}</span>
    {{/if}}

    {{#if final}}
      <span class="label label-danger">final</span>
    {{/if}}

    {{#if overwritten_from}}
      <span class="label label-danger">overwritten</span>
    {{/if}}

    <span class="label label-danger label-development">in-development</span>

    {{#if static}}
      <span class="label label-default">static</span>
    {{/if}}

    {{#if chainable}}
      <span class="label label-info">chainable</span>
    {{/if}}

    {{#if async}}
      <span class="label label-info">async</span>
    {{/if}}

    <span class="label label-info label-global">global</span>

    <div class="code-item-information">
      {{#if overwritten_from}}
        <span>
          Inherited from
          <a href="{{crossLinkRaw overwritten_from/class}}#method_{{overwritten_from/name}}">{{overwritten_from/class}}</a>
          {{#if foundAt}}
            but overwritten in
          {{/if}}
        </span>
      {{else}}
        {{#if extended_from}}
          <span>
            Inherited from:
            <a href="{{crossLinkRaw extended_from}}#method_{{name}}">{{extended_from}}</a>
          </span>
        {{else}}
          {{#providedBy}}
            <span>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</span>
          {{/providedBy}}
        {{/if}}
      {{/if}}
      {{#if deprecationMessage}}
        <span>Deprecated: {{deprecationMessage}}</span>
      {{/if}}
      {{#if providedBy}}
        {{#providedBy}}
          <span>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</span>
        {{/providedBy}}
      {{/if}}
    </div>

    <br><br>

    {{#if foundAt}}
      <small>
        <i class="fa fa-chevron-right"></i>&nbsp;&nbsp;Defined in: <a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>
      </small>
    {{/if}}

    {{#if since}}
      <small><i class="fa">&nbsp;&nbsp;&nbsp;</i>&nbsp;&nbsp;Available since <span class="badge">{{since}}</span></small>
    {{/if}}
  </div>

  <div class="code-item-description">
    {{{methodDescription}}}
  </div>

  {{#if trigger}}
  <div class="code-item-section">
    <h4>Triggers:</h4>
    <span class="code-item-trigger">{{trigger}}</span>
  </div>
  {{/if}}

  {{#if params}}
    <div class="code-item-section">
      <h4>Parameters:</h4>

      <ul class="params-list">
      {{#params}}
          <li class="param">
            <code class="param-name">{{name}}</code>
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

            {{#if optional}}<span class="label">Optional</span>{{/if}}
            {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

            <div class="param-description">
                {{{description}}}
            </div>

            {{#if props}}
              <ul class="params-list">
                {{#props}}
                  <li class="param">
                    <code class="param-name">{{name}}</code>
                    <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

                    {{#if optional}}<span class="label">Optional</span>{{/if}}
                    {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

                    <div class="param-description">
                      {{{description}}}
                    </div>

                    {{#if props}}
                      <ul class="params-list">
                        {{#props}}
                          <li class="param">
                            <code class="param-name">{{name}}</code>
                            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

                            {{#if optional}}<span class="label">Optional</span>{{/if}}
                            {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

                            <div class="param-description">
                              {{{description}}}
                            </div>
                          </li>
                        {{/props}}
                      </ul>
                    {{/if}}
                  </li>
                {{/props}}
              </ul>
            {{/if}}
          </li>
      {{/params}}
      </ul>
    </div>
  {{/if}}

  {{#return}}
    <div class="code-item-section">
      <h4>Returns:</h4>

      <div class="returns-description">
        {{#if description}}
          {{#if type}}
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>:
          {{/if}}
          {{{description}}}
        {{else}}
          {{#if type}}
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>:
          {{/if}}
        {{/if}}
      </div>
    </div>
  {{/return}}

  {{#example}}
    <div class="code-item-section">
      <h4>Example:</h4>
      <pre class="example-content prettyprint">
        {{{.}}}
      </pre>
    </div>
  {{/example}}
  <hr>
</div>