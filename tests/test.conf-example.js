/**
 * Edit the config settings here and save as `test.conf.js` in the same directory.
 */
var configs = {

  // Configures the test timeouts.
  timeout: 15000,

  // Configures the P2P (not MCU enabled) environment settings.
  p2p: {
    // The P2P (not MCU enabled) App key ID.
    appKey: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',
    // The P2P (not MCU enabled) App key secret.
    secret: 'xxxxxxxxxxxxx'
  },

  // Configures the MCU enabled environment settings.
  mcu: {
    // The MCU enabled App key ID.
    appKey: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',
    // The MCU enabled App key secret.
    secret: 'xxxxxxxxxxxxx'
  }

};